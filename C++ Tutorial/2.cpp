/*Variables definition.we can define our own type of variable. char used to store 1byte character. bool used for boolean , it takes true(1) or false(0) as input. float is used for decimal point value . double used for handle much more decimal value than float. */

#include <iostream>

using namespace std;

int main()
{
    int x;
    x=10;
    int y=20;
    int z=10,w=20;
    int sum=0;
    sum=z+w;
    char c='a';
    bool e=true;
    float f=10.1;
    cout << "Hello world!" << endl;
    cout << x << "\n";
    cout << y << "\n";
    cout << z << "\n";
    cout << w << "\n";
    cout << c << "\n";
    cout << e << "\n";
    cout << f << "\n";
    cout << sum << "\n";
    return 0;
}
