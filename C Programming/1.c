#include <stdio.h> /* preprocess command includes a header file to your project
 includes file name given . stdio is standard input output responsible for taking input and printing output like pritnf function.
  stdlib is included because main function is extended from it . main is entry point of a program . main is a built in function.
  function  has a return type and name and takes argument as input . we have to return something. every line ends with semicolon.
  every function start with open and close curly braces.
  Note : Indentation does not matter and it is case sensitive. */
#include <stdlib.h>

int main()
{
    printf("Hello world!\n");
    return 0;

}
